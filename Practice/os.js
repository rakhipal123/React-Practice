const os=require('os')

console.log('currently working on architeture', os.arch())
console.log('currently working on platform', os.platform())
console.log(`the information about the computer's cpu`, os.cpus())